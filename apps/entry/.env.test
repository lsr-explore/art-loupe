# End-to-end test settings. NOT secrets — this file is committed on purpose.
#
# The entry point is never authenticated, so it needs no credentials. It still gets a
# file because the suite runs with NODE_ENV=test (which makes Next skip `.env.local`),
# and any future test-only default belongs here rather than in the Playwright config.
DISABLE_HTTPS_UPGRADE="true"
