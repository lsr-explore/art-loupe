{
  "name": "@artloupe/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Art Loupe shared server-side auth — encrypted session, swappable AuthProvider seam, middleware guard. Demo super-user now; D6 OAuth swaps in behind the same interface.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./middleware": "./src/middleware.ts",
    "./ack": "./src/ack.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.114.0",
    "iron-session": "^9.0.1",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "next": "16.3.4",
    "typescript": "^6",
    "vitest": "^5.0.0"
  },
  "peerDependencies": {
    "next": "16.3.4"
  }
}
