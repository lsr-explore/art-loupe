/**
 * Shared TS contracts for Art Loupe.
 *
 * Intentionally empty: the art contracts are authored alongside the first agent
 * workflow. The package is kept so the workspace wiring, tsconfig, and the planned
 * codegen-from-Pydantic parity with `python/libs/schemas` stay in place.
 */
export {};
